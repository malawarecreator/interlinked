# Backend runtime dependencies
fastapi>=0.95.0
uvicorn[standard]>=0.20.0
openai>=0.27.0
# Keep pydantic v1.x for compatibility with some FastAPI versions; adjust if you
# intentionally use pydantic v2 and a compatible FastAPI release.
pydantic>=1.10,<2

# HTTP client used by TestClient and for any outgoing requests
requests>=2.28.0

# Optional: environment variable loader (useful in development)
python-dotenv>=1.0.0

# Test deps
pytest>=7.0
pytest-cov>=4.0

# Allow using httpx directly if you write async tests or clients
httpx>=0.24.0
